the notebook code works in google colab
if you want to run it in vs code change the directories 
and install python 3.10v coz it's efficient for ML models
install tensorflow before running the program,
run each cell---->train the model with your own dataset------> save the models
now select the any model(i prefer Xception.h5----more accurate) run it----> choose the image---->result
you will aslo get confusion matrix of each model, also accuracy and loss rate.
